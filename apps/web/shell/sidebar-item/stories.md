# SidebarItem — Usage

```tsx
import { SidebarItem } from '@/shell/sidebar-item';

<SidebarItem label="Dashboard" active onSelect={() => {}} />
<SidebarItem label="Projects" onSelect={() => {}} />
```
